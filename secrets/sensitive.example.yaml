---
# Template for secrets/sensitive.sops.yaml.
#
# TWO KEYS, for now. Read the list as a description of the stack: Caddy needs
# no credential — its certificate and key are files under certificates/, never
# tracked, handled the way the estate's Grafana leaf is — step-ca needs
# exactly one, the password that unlocks its intermediate key, Immich needs
# one, the password its server presents to its own Postgres, and Home
# Assistant needs NONE from here, which is not the same as needing none. Its
# device credentials — the Hue application key, the Ring token, everything a
# config flow produces — are written by Home Assistant into its own store in
# the home-assistant-config volume, and no environment variable or rendered
# file could carry them in; compose.yaml and ADR-0035 say what protects them
# instead. Vaultwarden's admin token arrives here with its service (#131);
# this file grows with the tier and the compose file's `${VAR:?}` guards say
# what is required, so `make render STACK=sensitive` asks for what this
# stack needs and nothing else.
#
# ---------------------------------------------------------------------------
# Create this ON TRINITY, not on the monitoring host
# ---------------------------------------------------------------------------
#
#   make secrets-init STACK=sensitive    # first time only — creates the age keypair
#   make secrets-edit STACK=sensitive    # opens the encrypted file in $EDITOR
#
# secrets-init writes trinity's public key into the `sensitive` rule in
# .sops.yaml — its own rule, so that the tier's host can open the tier's
# secrets and nothing else: not the SNMP communities, not the Grafana admin
# password. It refuses if the key it would write is already a recipient of
# another rule. The technical second's recipient (ADR-0024, #294) is added to
# the same rule with `make secrets-add-recipient PUBKEY=... STACK=sensitive`
# on the day the tier holds real data, which is what ADR-0023 asks for the
# household's secrets.
#
# The password below is the one given to `step ca init` when the intermediate
# is minted on the monitoring host; the two must match or step-ca cannot open
# its own key. Long and random; it is typed nowhere after that.
STEPCA_PASSWORD: change-me-to-something-long

# Immich's database password (#132). Read twice from the same line: Postgres
# sets it as the superuser's password when the volume is first initialised
# and never reads it again, and immich-server presents it on every
# connection — so a change here after first start changes what the server
# sends and not what the database expects, and the fix is ALTER USER inside
# the container, not a re-render. Letters and digits only: Immich's own
# install page asks for `A-Za-z0-9` because the value travels through
# compose interpolation and a connection string. `make gen-secret` produces
# exactly that alphabet.
IMMICH_DB_PASSWORD: change-me-to-something-long
